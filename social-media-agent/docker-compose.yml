version: "3.9"

services:
  social-media-manager:
    build: .
    restart: always
    ports:
      - "${WEBHOOK_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    command: python run_agent.py
