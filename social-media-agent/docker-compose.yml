version: "3.9"

services:
  lixen-ai-agent:
    build: .
    restart: always
    ports:
      - "${WEBHOOK_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    command: python run_agent.py
